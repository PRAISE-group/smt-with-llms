descriptions = [
    [
        "Closed box function 'foo_cb' takes 2 arguments (sum, i) and returns sum + (i * i). Uses BitVec 16 Domain.",
    ],
    [
        "Closed box function 'isprime_cb' takes 1 argument (N) and returns whether N is a prime number or not. Uses BitVec 16 Domain.",
    ],
    [
        "Closed box function 'retmod_cb' takes 2 arguments (a, b) and returns a % b Uses BitVec 16 Domain.",
        "Closed box function 'gcd_cb' takes 2 arguments (a, b) and returns the greatest common divisor between a and b. Uses BitVec 16 Domain.",
    ],
    [
        "Closed box function 'add_sum_cb' takes 2 arguments (a, b) and returns a + b. Uses BitVec 16 Domain.",
        "Closed box function 'fib_cb' takes 1 argument (N) and returns the N-th Fibonacci number. Uses BitVec 16 Domain.",
    ],
    [
        "Closed box function 'binpow_cb' takes 2 arguments (a, b) and returns a raised to the power b. Uses BitVec 16 Domain.",
    ],
    [
        "Closed box function 'pre_cb' takes 3 arguments (a, b, c) and returns `a` left shifted by `c` in a loop while `a` is greater than `b`. Uses BitVec 16 Domain.",
        "Closed box function 'lshift_cb' takes 2 arguments (a, b) and returns a >> b (left shift). Uses BitVec 16 Domain.",
    ],
    [
        "Closed box function builtin_ctz_cb takes 3 arguments (a, b, c) and retunrs a + (b << c). Uses BitVec 16 Domain.",
    ],
    [
        "Closed box function 'shift_cb' takes 2 arguments (a, b) and returns a << b (right shift). Uses BitVec 16 Domain.",
        "Closed box function 'icbrt_cb' takes 1 arguments (N) and returns integer cube root of N as a fixed BV16. Uses BitVec 16 Domain.",
    ],
    [
        "Closed box function 'cube_cb' takes 1 arguments (N) and returns cube N * N * N. Uses BitVec 16 Domain.",
    ],
    [
        "Closed box function 'multiply_cb' takes 2 arguments (a, b) and returns a * b Uses BitVec 16 Domain.",
        "Closed box function 'factorial_cb' takes 1 argument (N) and returns the factorial value of N. Uses BitVec 16 Domain.",
    ],
    [
        "Closed box function 'foo_cb' takes 2 arguments (sum, i) and returns sum + (i * i * i). Uses BitVec 16 Domain.",
    ],
]

(set-logic AUFLIA)
(declare-const x Int)
(declare-const y Int)
(declare-fun foo_cb (Int Int) Int)
(assert (< x y))
(assert (not (= (+ x y) 8) ) )
(assert (or (= (foo_cb x y) 8) (= (foo_cb x y) 7) ))
(check-sat)

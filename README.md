# smt-with-llms

## Setup

- Install `uv` package manager: [uv](https://docs.astral.sh/uv/getting-started/installation/)

```bash
$ curl -LsSf https://astral.sh/uv/install.sh | sh
$ uv python install 3.12
```

To run a python file use `uv run ...` instead of `python ...`.